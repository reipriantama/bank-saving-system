// TODO : jangan lupa update layout ini sesuaikan dengan figma

import React from "react";

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
  return <div>{children}</div>;
};

export default AuthLayout;
